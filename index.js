/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Restaurants from './src/pages/Restaurant'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Restaurants);
